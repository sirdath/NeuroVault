#  Cross-platform release builds for NeuroVault.
#
#  Triggered by pushing a tag matching `v*` (e.g. `git tag v0.1.2 &&
#  git push --tags`) or manually via the Actions tab. Builds Windows +
#  macOS (arm64 + Intel) + Linux in parallel and uploads the resulting
#  installers to a draft GitHub Release.
#
#  Why a draft (not auto-publish): release notes get a human pass and we
#  spot a broken artifact before users grab it. Edit + publish from the
#  Releases UI when the matrix finishes green.
#
#  No code signing. Mac users hit Gatekeeper on first launch (right-click
#  -> Open -> Open Anyway), Windows users hit SmartScreen. README
#  documents both. Notarization + EV cert is a separate decision —
#  Apple Dev = $99/yr, Windows EV = ~$200-400/yr; revisit if there's
#  demand.
#
#  Reproducibility note: SQLITE_VEC_VERSION resolves "latest" from the
#  upstream releases API on every run. If asg017/sqlite-vec ever ships
#  a breaking schema change we'd want to pin a known-good version
#  here; for now floating-latest is the simpler default.
#
#  Replaces the pre-v0.1.0 workflow that built a Python sidecar via
#  PyInstaller. Sidecar was retired in the Rust migration; advanced
#  features now spawn `python -m neurovault_server.cli.<job>` on demand.

name: Release

on:
  push:
    tags:
      # `v[0-9]*` matches semver tags like v0.1.5 / v1.0.0 but not
      # the VS Code extension's `vscode-v*` tags, which have their own
      # release-vscode.yml pipeline.
      - "v[0-9]*"
  workflow_dispatch:
    # Manual trigger for testing the workflow without a real tag.
    # Uses the most recent commit on whatever branch is selected.

jobs:
  build:
    name: Build (${{ matrix.os-label }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            os-label: windows
            rust-target: x86_64-pc-windows-msvc
            vec_os: windows
            vec_arch: x86_64
            # Upstream sqlite-vec only ships windows builds as tar.gz (no
            # zip variant). Git Bash on the Windows runner has tar built
            # in, so the else-branch of the extract step handles it the
            # same way as macOS / Linux.
            vec_archive_ext: tar.gz
          - os: macos-14   # Apple Silicon (M-series)
            os-label: macos-arm64
            rust-target: aarch64-apple-darwin
            vec_os: macos
            vec_arch: aarch64
            vec_archive_ext: tar.gz
          - os: macos-13   # Intel
            os-label: macos-x64
            rust-target: x86_64-apple-darwin
            vec_os: macos
            vec_arch: x86_64
            vec_archive_ext: tar.gz
          - os: ubuntu-22.04   # 22.04 has webkit2gtk-4.1; 24.04 phased it out
            os-label: linux-x64
            rust-target: x86_64-unknown-linux-gnu
            vec_os: linux
            vec_arch: x86_64
            vec_archive_ext: tar.gz
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write   # creates / updates GitHub Releases
    steps:
      - uses: actions/checkout@v4

      # --- Linux system deps -------------------------------------------------
      # Tauri's canonical apt list. webkit2gtk = embedded webview;
      # libayatana-appindicator = system tray; librsvg2 = SVG icon
      # rendering; patchelf is needed by the AppImage bundler.
      - name: Install Linux deps
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            patchelf

      # --- Toolchains --------------------------------------------------------
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}

      # Cache cargo's target dir per platform. The release profile uses
      # lto=fat + codegen-units=1, so a cold build is 4-6 minutes.
      # With this cache, incremental rebuilds drop to ~90s.
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
          key: ${{ matrix.rust-target }}

      # --- vec0 binary -------------------------------------------------------
      # The sqlite-vec extension we load at runtime. Different filename
      # per platform: vec0.dll / vec0.dylib / vec0.so. tauri.conf.json
      # uses a glob (resources/vec0.*) so whichever file is present at
      # build time gets bundled into the installer. We resolve "latest"
      # from the upstream releases API so this workflow doesn't rot when
      # a new sqlite-vec ships.
      - name: Download sqlite-vec extension
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p src-tauri/resources

          # Resolve latest sqlite-vec release tag without jq (Windows
          # runners don't have it preinstalled).
          VERSION=$(curl -sSL https://api.github.com/repos/asg017/sqlite-vec/releases/latest \
            | grep '"tag_name"' | head -n1 | cut -d'"' -f4)
          if [ -z "$VERSION" ]; then
            echo "::error::failed to resolve sqlite-vec latest version"
            exit 1
          fi
          echo "Using sqlite-vec ${VERSION}"

          # Asset name pattern: sqlite-vec-{ver}-loadable-{os}-{arch}.{ext}
          # ver in the asset is the tag without the leading 'v'.
          STRIPPED="${VERSION#v}"
          ASSET="sqlite-vec-${STRIPPED}-loadable-${{ matrix.vec_os }}-${{ matrix.vec_arch }}.${{ matrix.vec_archive_ext }}"
          URL="https://github.com/asg017/sqlite-vec/releases/download/${VERSION}/${ASSET}"
          echo "Fetching ${URL}"
          curl -fL -o /tmp/vec0-archive "${URL}"

          # Extract just the vec0 binary into src-tauri/resources/.
          # Both .tar.gz and .zip ship the file at the archive root.
          if [ "${{ matrix.vec_archive_ext }}" = "zip" ]; then
            unzip -o /tmp/vec0-archive -d src-tauri/resources/
          else
            tar -xzf /tmp/vec0-archive -C src-tauri/resources/
          fi
          ls -lh src-tauri/resources/

      # --- Frontend ----------------------------------------------------------
      - name: Install npm deps
        run: npm ci

      # --- Release notes from CHANGELOG.md ----------------------------------
      # Pulls the section under `## [<version>]` for the current tag so
      # the release body matches the changelog without hand-copying.
      # Falls back to a generic message if the section isn't there yet.
      - name: Extract CHANGELOG section for this tag
        id: changelog
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          body=""
          if [ -f CHANGELOG.md ]; then
            body=$(awk -v ver="$VERSION" '
              $0 ~ "^## \\[" ver "\\]" { flag=1; next }
              flag && /^## \[/ { flag=0 }
              flag { print }
            ' CHANGELOG.md)
          fi
          if [ -z "$body" ]; then
            body="See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for the full release notes."
          fi
          {
            echo "body<<CHANGELOG_EOF"
            echo "$body"
            echo "CHANGELOG_EOF"
          } >> "$GITHUB_OUTPUT"

      # --- Tauri build + draft release --------------------------------------
      # tauri-action runs `npx tauri build --target <rust-target>`,
      # picks up each platform's bundle output (.exe, .dmg, .deb,
      # .AppImage), creates / updates a single draft Release for the tag,
      # and attaches every matrix job's artifacts to it.
      - name: Build + upload to draft release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: --target ${{ matrix.rust-target }}
          tagName: ${{ github.ref_name }}
          releaseName: "NeuroVault ${{ github.ref_name }}"
          releaseBody: |
            ## NeuroVault ${{ github.ref_name }}

            ${{ steps.changelog.outputs.body }}

            ---

            ### Downloads

            - **Windows**: `NeuroVault_*_x64-setup.exe` (NSIS installer)
            - **macOS (Apple Silicon)**: `NeuroVault_*_aarch64.dmg`
            - **macOS (Intel)**: `NeuroVault_*_x64.dmg`
            - **Linux**: `neurovault_*_amd64.AppImage` or `neurovault_*_amd64.deb`

            ### First-launch warnings

            The macOS and Windows builds aren't code-signed. You'll see a
            warning the first time you open the app:

            - **macOS** ("damaged, move to trash"): right-click the .app,
              choose Open, click Open Anyway. Or run
              `xattr -cr /Applications/NeuroVault.app` once.
            - **Windows SmartScreen**: click "More info", then "Run anyway".

            Full changelog: https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md
          releaseDraft: true
          prerelease: false
